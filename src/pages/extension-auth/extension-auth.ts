// This page exists in case the extension needs to show a local auth bridge.
// However, typically we open the hosted clipiq.vercel.app/extension-auth directly.

console.log('Local extension auth bridge loaded.');
